@model List<DAL.EF.DANHMUC>

<h4 class="my-4">Danh Mục Sản Phẩm</h4>
<ul class="list-group">
        @foreach (var item in Model)
        {
            <li class="list-group-item"><a style="color:gray" href="/san-pham/@item.Meta_Title-@item.CategoryID">@item.Name</a></li>
        }
</ul>